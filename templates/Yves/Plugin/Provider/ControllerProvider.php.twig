<?php

namespace {{ namespace }};

use Pyz\Yves\Application\Plugin\Provider\AbstractYvesControllerProvider;
use Silex\Application;

class {{ classname }} extends AbstractYvesControllerProvider
{

    const {{ bundle | upper }}_INDEX = '{{ bundle | lower }}{# @todo actually dashed-case #}-index';

    /**
     * @param \Silex\Application $app
     *
     * @return void
     */
    protected function defineControllers(Application $app)
    {
        $this->createGetController('/{{ bundleDashed }}', static::{{ bundle | upper }}_INDEX, '{{ bundle }}', 'Index', 'index');
    }

}
